"use client";
import React from 'react';
import ProfileContent from '@/app/components/ProfileContent';

const MyProfilePage = () => {
    return (
        <ProfileContent title="My Profile" showBackButton={true} />
    );
};

export default MyProfilePage;
